name: Build & Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Daily rebuild at 6 AM EST (11 AM UTC)
  schedule:
    - cron: '0 11 * * *'
  # Rebuild on HubSpot blog post publish/update
  repository_dispatch:
    types: [hubspot_blog_update]
  # Allow manual trigger
  workflow_dispatch:

env:
  HUBSPOT_ACCESS_TOKEN: ${{ secrets.HUBSPOT_ACCESS_TOKEN }}
  SITE_URL: ${{ secrets.SITE_URL || 'https://attorneyassistant.com' }}
  GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_MAPS_API }}
  BABYLOVEGROWTH: ${{ secrets.BABYLOVEGROWTH }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npm run build

      - name: Deploy to FTP
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ${{ secrets.FTP_SERVER_DIR || '/' }}
